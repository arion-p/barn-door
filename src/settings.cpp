#include "defines.h"
#include "settings.h"


float InitialOpening = 2.1;       // Initial opening of barn doors when switched on 
                                  // (distance between two pivot points in cm)
float MaximumOpening = 16.5;      // Maximum distance to allow barn doors to open (30 deg == 2 hours)


void readSettings() {

}

void saveSettings() {
    
}