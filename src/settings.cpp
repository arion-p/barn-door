#include "defines.h"
#include "settings.h"


float InitialOpening = 2.1;       // Initial opening of barn doors at home position
                                  // (distance between the two pivot points in cm)
float MaximumOpening = 16.5;      // Maximum distance to allow barn doors to open (30 deg == 2 hours)
long PlanAheadTrueTimeMs = DEFAULT_PLANAHEAD_TIME;  // Real time in ms between plans
long PlanAheadSysTimeMs = DEFAULT_PLANAHEAD_TIME;   // System time in ms between plans


void readSettings() {

}

void saveSettings() {

}